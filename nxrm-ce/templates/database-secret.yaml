{{- if and .Values.secret.dbSecret.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ template "nexus.name" . }}-dbsecret
  namespace: {{ default .Release.Namespace .Values.namespaceOverride | quote }}
  labels:
{{ include "nexus.labels" . | nindent 4 }}
  {{- if .Values.nexus.extraLabels }}
    {{- with .Values.nexus.extraLabels }}
      {{ toYaml . | nindent 4 }}
    {{- end }}
  {{- end }}
type: Opaque
stringData:
  db-host: {{ .Values.secret.db.host }}
  db-user: {{ .Values.secret.db.user }}
  db-password: {{ .Values.secret.db.password }}
{{- end }}
